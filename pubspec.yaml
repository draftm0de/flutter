name: draftmode
description: Facade of all DraftMode flutter packages
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.10.0"

# The facade depends on the real packages
dependencies:
  flutter:
    sdk: flutter
  draftmode_worker:
    path: ../worker

repository: https://github.com/draftm0de/flutter
issue_tracking: https://github.com/draftm0de/flutter/issues
